#!/bin/bash

# رنگ‌ها
GREEN='\033[0;32m'
YELLOW='\033[1;33m'
RED='\033[0;31m'
NC='\033[0m'

echo -e "${GREEN}====================================${NC}"
echo -e "${GREEN}🚀 در حال راه‌اندازی ربات...${NC}"
echo -e "${GREEN}====================================${NC}"

# چک کردن وجود پایتون
if ! command -v python3 &> /dev/null; then
    echo -e "${RED}❌ پایتون نصب نیست!${NC}"
    exit 1
fi

# چک کردن وجود فایل .env
if [ ! -f .env ]; then
    echo -e "${YELLOW}⚠️ فایل .env پیدا نشد!${NC}"
    echo -e "از روی نمونه می‌سازم..."
    cp .env.example .env
    echo -e "${GREEN}✅ فایل .env ساخته شد. لطفاً توکن‌ها رو وارد کن!${NC}"
    exit 1
fi

# نصب کتابخانه‌ها
echo -e "${YELLOW}📦 نصب کتابخانه‌ها...${NC}"
pip3 install -r requirements.txt

# اجرای ربات
echo -e "${GREEN}✅ ربات در حال اجراست...${NC}"
python3 start.py
